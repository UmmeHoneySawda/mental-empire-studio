### Shared visual style

Premium desktop creator application UI for **Mental Empire Studio**, Windows-first Electron application, sophisticated dark interface inspired by Linear, Descript, CapCut Desktop and modern professional editing software, deep charcoal background, layered graphite panels, subtle glass surfaces, thin neutral borders, bright electric cyan accent with restrained violet highlights, clean Space Grotesk-style typography, compact professional spacing, sharp icons, realistic desktop software screenshot, high information density without clutter, excellent contrast, polished production-ready UX, 16:10 desktop canvas, no browser chrome, no device mockup, no people, no decorative illustration, no fake logos, no watermark.

* * *

### Prompt 1 — Integration dashboard

Create one standalone desktop application screen showing the **OpenMontage Integration Dashboard** inside Mental Empire Studio.

Left navigation contains: Home, Projects, Download, Compose, Automations, Render Queue, OpenMontage, Settings.

The OpenMontage page shows a large readiness card with status **“OpenMontage Ready”**, installed version, agent runner, Backlot connection, Remotion status and HyperFrames status. Beneath it, show a capability matrix with Open Footage, Archive Footage, Pexels, Pixabay, Unsplash, Remotion, HyperFrames and Agent Runner, each with clear available or limited status indicators.

Include three primary workflow cards: **Automatic**, **Mental Empire Studio**, and **OpenMontage**. Automatic is selected and visually recommended. A smaller section shows recent productions with project title, selected pipeline, runtime, progress and final status.

The screen should feel like a professional system-control center, not a marketing landing page. Use realistic data and short readable interface text. Preserve the shared visual style exactly.

* * *

### Prompt 2 — New production setup

Create one standalone desktop application screen showing the **New Production** workflow inside Mental Empire Studio.

Use a structured multi-step layout with a vertical step rail:

1. Source

2. Media Control

3. Production Style

4. Composition

5. Approvals

6. Output

7. Review

The current step is **Media Control**.

Show four large selectable options:

* Preserve selected media

* Improve weak selections

* Fill missing scenes

* Let OpenMontage choose everything

The selected option is **Improve weak selections**, with an explanation that user-locked assets will remain unchanged.

On the right, include a project summary panel showing narration loaded, 65 scenes, 38 images, 12 video clips, 16:9, 1080p, English narration, and export folder. Bottom actions: Back and Continue.

Professional editing-tool interface, precise alignment, no oversized marketing elements. Preserve the shared visual style exactly.

* * *

### Prompt 3 — Automatic workflow decision

Create one standalone desktop application screen showing an **Automatic Workflow Decision** summary before launching a production.

The top heading reads **“Production plan ready”**.

Show a decision card:

* Engine: OpenMontage

* Pipeline: Hybrid

* Composition: Remotion

* Authoring mode: Atelier

* Estimated approval gates: 3

* Fallback: Mental Empire Studio

* Export: D:\Mental Empire\Exports

Include a clearly written section titled **“Why this engine was selected”** with compact reasons: real footage requested, advanced stock selection enabled, editable composition required, OpenMontage health checks passed.

Display a provider availability strip for Archive.org, Wikimedia Commons, Pexels, Unsplash, Groq, FFmpeg and Remotion.

Include a warning panel stating that HyperFrames is available but Remotion is recommended for this project. Bottom-right primary button: **Start Production**. Secondary button: Edit Plan.

The interface must communicate confidence and transparency. Preserve the shared visual style exactly.

* * *

### Prompt 4 — Live production progress

Create one standalone desktop application screen showing a **Live OpenMontage Production** running inside Mental Empire Studio.

Top bar shows project title, status **Running**, elapsed time and Pause, Cancel and View Logs controls.

Main center area shows a horizontal production-stage timeline:

Preparing → Research → Script → Scene Plan → Assets → Edit → Compose → Export

Research, Script and Scene Plan are complete. Assets is active at 62%.

Below the timeline, show the active operation:

**Selecting footage for Scene 41 of 65**

Include a large scene card with a cinematic stock-footage thumbnail, search queries, provider source, relevance score, license status and buttons for Lock, Replace and Preview.

Right sidebar shows:

* Current stage

* Completed scenes

* API cost

* Last checkpoint

* Active provider

* Retry count

* Fallback status

Bottom area contains a scrolling activity log with short, readable production events. Preserve the shared visual style exactly.

* * *

### Prompt 5 — Storyboard approval gate

Create one standalone desktop application screen showing an **Asset Approval Required** state.

The screen contains a cinematic storyboard filmstrip with eight scene cards in two rows. Each card has a thumbnail, scene number, short narration excerpt, selected provider, asset cost, quality score and lock indicator.

The first six scenes are approved. Scene 7 has two alternative takes displayed. Scene 8 has a warning for weak visual relevance.

At the top, show:

**Assets stage complete — approval required**

Include summary metrics:

* 65 scenes

* 61 ready

* 3 alternatives

* 1 warning

* Cost so far

* Projected render cost

Right-side review panel includes the agent’s recommendation and a text field labeled **Revision instructions**.

Bottom actions:

* Approve Assets

* Request Changes

* Stop Production

This should look like a real editorial approval interface, not a generic gallery. Preserve the shared visual style exactly.

* * *

### Prompt 6 — Remotion versus HyperFrames

Create one standalone desktop application modal inside Mental Empire Studio comparing **Remotion** and **HyperFrames**.

Use two equally sized professional comparison cards.

Remotion card:

* React-based composition

* Best for scene-driven storytelling

* Strong captions and data visuals

* Editable project output

* Recommended badge

HyperFrames card:

* HTML, CSS and GSAP

* Best for kinetic typography

* Strong motion-graphics workflows

* Editable workspace output

* Available badge

Include concise project-specific tradeoffs under each option.

At the bottom, show the agent recommendation:

**“Remotion is recommended because this project combines narration, stock footage, captions and scene-based editing.”**

Buttons:

* Use Remotion

* Use HyperFrames

* Return to Automatic

No excessive graphics. The comparison must feel neutral and trustworthy. Preserve the shared visual style exactly.

* * *

### Prompt 7 — Recoverable interruption

Create one standalone desktop application screen showing a production that was recovered after the app restarted.

Top status banner reads:

**“Existing production found — resumed from Assets stage”**

Show a recovery timeline with:

* Last successful checkpoint

* Application closed

* OpenMontage process detected

* Backlot reconnected

* Production resumed

The main progress area shows Scene 44 of 65 and a message explaining that completed scenes were not regenerated.

Include a compact checkpoint history panel showing Script completed, Scene Plan completed, Assets in progress and last saved time.

Controls:

* Continue Monitoring

* Pause

* Open Project Folder

* View Recovery Details

The experience should feel reassuring and technically reliable. Preserve the shared visual style exactly.

* * *

### Prompt 8 — Failure and MES fallback

Create one standalone desktop application screen showing a fatal OpenMontage rendering failure followed by automatic Mental Empire Studio fallback.

Use a clear two-part status layout.

Upper section:

**OpenMontage attempt failed**

Show:

* Failed stage: Compose

* Runtime: Remotion

* Error: renderer process exited

* Retry attempts: 3

* Checkpoints preserved

* Sentry report created

* Error reference ID

Include actions:

* View Full Trace

* Open OpenMontage Project

* Copy Recovery Prompt

Lower section:

**MES fallback is now running**

Show the original MES production pipeline restarting from preparation, with a progress bar at 18%.

Clearly state that the OpenMontage files were preserved and no project data was deleted.

The interface must communicate failure honestly without appearing alarming or chaotic. Preserve the shared visual style exactly.

* * *

### Prompt 9 — Completed production outputs

Create one standalone desktop application screen showing a successfully completed OpenMontage production inside Mental Empire Studio.

Top success header reads:

**Production complete**

Include a large 16:9 video preview with timeline controls.

Show output cards for:

* Final MP4

* Editable Remotion Project

* Captions

* Production Assets

* Decision Log

* Render Report

Each card includes file size, location and an appropriate action.

Primary actions:

* Play Final Video

* Open Editable Project

* Open Export Folder

Secondary section shows completion details:

* Engine: OpenMontage

* Pipeline: Hybrid

* Runtime: Remotion

* Duration

* Resolution

* Total production time

* Final cost

* Quality validation passed

Preserve the shared visual style exactly.

* * *

### Prompt 10 — OpenMontage settings

Create one standalone desktop application screen showing **Settings → OpenMontage** inside Mental Empire Studio.

Organize the screen into professional settings sections:

**Installation**

* OpenMontage repository location

* Installed version

* Update compatibility

* Validate installation button

**Agent Runner**

* Runner selection

* Executable location

* Managed mode toggle

* Assisted fallback toggle

**Capabilities**

* Remotion

* HyperFrames

* Backlot

* FFmpeg

* Python environment

**Credentials**

* Pexels

* Pixabay

* Unsplash

* OpenAI

* Google

* ElevenLabs

* Other providers

Credential values must be hidden.

**Reliability**

* Retry limit

* Stall timeout

* Automatic MES fallback

* Preserve failed projects

* Send sanitized errors to Sentry

Include a final **Run Full Health Check** button and a compact health-results panel. Preserve the shared visual style exactly.
